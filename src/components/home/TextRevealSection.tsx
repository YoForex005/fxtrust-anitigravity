'use client';

import { TextRevealByWord } from "@/components/ui/text-reveal";

export default function TextRevealSection() {
  return (
    <TextRevealByWord
      text="FxTrusts delivers end-to-end forex technology. PropFirm platforms, AI trading, CRM systems, trade copiers — all whitelabeled and launched in weeks, not months."
    />
  );
}
